<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.br.notice.mapper.SystemNoticeMapper">

    <resultMap id="BaseResultMap" type="com.br.notice.entity.SystemNotice">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="activity_id" property="activityId"/>
        <result column="popup_start_time" property="popupStartTime"/>
        <result column="popup_end_time" property="popupEndTime"/>
        <result column="status" property="status"/>
        <result column="create_admin_id" property="createAdminId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="getById" resultMap="BaseResultMap">
        SELECT * FROM system_notice WHERE id = #{id}
    </select>

    <select id="listAll" resultMap="BaseResultMap">
        SELECT * FROM system_notice ORDER BY create_time DESC
    </select>

    <select id="listActiveUnread" resultMap="BaseResultMap">
        SELECT n.*
        FROM system_notice n
        LEFT JOIN user_notice_read r
          ON n.id = r.notice_id AND r.user_id = #{userId}
        WHERE n.status = 1
          AND n.popup_start_time &lt;= NOW()
          AND n.popup_end_time &gt;= NOW()
          AND r.id IS NULL
        ORDER BY n.create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO system_notice (title, content, activity_id, popup_start_time, popup_end_time, status, create_admin_id)
        VALUES (#{title}, #{content}, #{activityId}, #{popupStartTime}, #{popupEndTime}, #{status}, #{createAdminId})
    </insert>

    <update id="update">
        UPDATE system_notice
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="activityId != null">activity_id = #{activityId},</if>
            <if test="popupStartTime != null">popup_start_time = #{popupStartTime},</if>
            <if test="popupEndTime != null">popup_end_time = #{popupEndTime},</if>
            <if test="status != null">status = #{status},</if>
            <if test="createAdminId != null">create_admin_id = #{createAdminId},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>
